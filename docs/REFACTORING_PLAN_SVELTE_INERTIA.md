# Talkk Web Frontend 리팩토링 계획서
## Rails + Svelte 5 + Vite + Inertia.js (Whispr-style UI)

**작성일**: 2026-02-12
**프로젝트**: `/Users/seunghan/dev/talkk/api/`
**목표**: 기존 Rails API-only 백엔드를 Rails + Svelte 5 + Inertia.js 풀스택 웹앱으로 리팩토링
**디자인 레퍼런스**: [Whispr (위스퍼)](https://www.whispr.co.kr/) - 익명 음성 대화 앱

---

## 1. 프로젝트 개요

### 1.1 현재 상태
- **Backend**: Rails 8.1 API-only (`--api` mode)
- **Frontend**: Flutter 모바일 앱 (별도 repo: `talkk/flutter/`)
- **DB**: PostgreSQL (15 tables), Redis + Sidekiq
- **인증**: JWT 기반
- **배포**: Render.com (Singapore)

### 1.2 목표 상태
- **Backend**: Rails 8.1 풀스택 (API + Inertia.js SSR)
- **Frontend**: Svelte 5 + Vite + Inertia.js (웹 대시보드/관리)
- **디자인**: Whispr-inspired 하늘색 테마, 모바일 퍼스트 반응형
- **기존 API 유지**: Flutter 앱과의 하위 호환성 보장

### 1.3 리팩토링 범위
| 영역 | 변경 사항 |
|------|----------|
| Rails config | API-only → Full-stack (views, assets 활성화) |
| Gemfile | + inertia_rails, vite_rails 추가 |
| package.json | + svelte, @inertiajs/svelte, vite |
| Frontend | 신규 Svelte 5 pages/components 생성 |
| Routes | 기존 API 라우트 유지 + Inertia 웹 라우트 추가 |
| Controllers | API controllers 유지 + Web controllers 추가 |

---

## 2. 디자인 시스템

### 2.1 색상 팔레트 (Sky Blue 테마)

Whispr의 보라색 → **하늘색(Sky Blue)** 계열로 변환. 2025-2026 트렌드인 "Digital Lavender meets Ocean Blue" 컨셉.

```
Primary Colors (메인 하늘색 계열)
├── sky-50:    #F0F9FF    (배경, 카드 hover)
├── sky-100:   #E0F2FE    (서브 배경)
├── sky-200:   #BAE6FD    (보더, 디바이더)
├── sky-300:   #7DD3FC    (비활성 요소)
├── sky-400:   #38BDF8    (호버 상태)
├── sky-500:   #0EA5E9    ★ 메인 브랜드 컬러
├── sky-600:   #0284C7    (버튼 hover)
├── sky-700:   #0369A1    (Active 상태)
├── sky-800:   #075985    (텍스트 강조)
└── sky-900:   #0C4A6E    (다크 요소)

Accent Colors (포인트)
├── cyan-400:  #22D3EE    (알림 뱃지, 온라인 표시)
├── teal-500:  #14B8A6    (성공 상태)
├── rose-500:  #F43F5E    (에러, 신고)
├── amber-400: #FBBF24    (경고, 별점)
└── violet-500:#8B5CF6    (프리미엄 기능)

Neutral Colors (중립)
├── slate-50:  #F8FAFC    (페이지 배경)
├── slate-100: #F1F5F9    (카드 배경)
├── slate-200: #E2E8F0    (보더)
├── slate-400: #94A3B8    (placeholder 텍스트)
├── slate-600: #475569    (보조 텍스트)
├── slate-800: #1E293B    (본문 텍스트)
└── slate-900: #0F172A    (제목)

Gradient (그라데이션)
├── hero:      sky-500 → cyan-400     (헤더, CTA)
├── card:      sky-50 → white         (카드 배경)
├── button:    sky-500 → sky-600      (프라이머리 버튼)
└── dark:      slate-800 → slate-900  (다크모드 배경)
```

### 2.2 타이포그래피

```
Font Family
├── Heading:   "Pretendard" (한글), "Inter" (영문)
├── Body:      "Pretendard" (한글), "Inter" (영문)
└── Mono:      "JetBrains Mono" (코드)

Font Sizes (Tailwind Scale)
├── xs:    0.75rem (12px)  - 캡션, 타임스탬프
├── sm:    0.875rem (14px) - 보조 텍스트
├── base:  1rem (16px)     - 본문
├── lg:    1.125rem (18px) - 서브 제목
├── xl:    1.25rem (20px)  - 섹션 제목
├── 2xl:   1.5rem (24px)   - 페이지 제목
├── 3xl:   1.875rem (30px) - 히어로 서브
└── 4xl:   2.25rem (36px)  - 히어로 메인
```

### 2.3 핵심 UI 컴포넌트 (Whispr 참고)

```
Components (Svelte 5)
├── Layout/
│   ├── AppShell.svelte          # 전체 레이아웃 셸
│   ├── Sidebar.svelte           # 좌측 네비게이션 (데스크톱)
│   ├── BottomNav.svelte         # 하단 네비게이션 (모바일)
│   ├── Header.svelte            # 상단 헤더바
│   └── PageContainer.svelte     # 페이지 콘텐츠 래퍼
│
├── Common/
│   ├── Button.svelte            # 다양한 variant (primary, ghost, danger)
│   ├── Avatar.svelte            # 익명 아바타 (동물/색상 조합)
│   ├── Badge.svelte             # 알림 뱃지, 상태 뱃지
│   ├── Card.svelte              # 기본 카드 컴포넌트
│   ├── Modal.svelte             # 모달 다이얼로그
│   ├── Toast.svelte             # 토스트 알림
│   ├── EmptyState.svelte        # 빈 상태 일러스트
│   └── LoadingSpinner.svelte    # 로딩 인디케이터
│
├── Voice/
│   ├── VoiceRecorder.svelte     # 음성 녹음 UI (60초 제한)
│   ├── VoicePlayer.svelte       # 음성 재생 (파형 시각화)
│   ├── VoiceWaveform.svelte     # 실시간 파형 애니메이션
│   └── RecordButton.svelte      # 녹음 버튼 (Whispr 스타일)
│
├── Chat/
│   ├── ChatBubble.svelte        # 채팅 말풍선 (음성/텍스트)
│   ├── ChatList.svelte          # 대화 목록
│   ├── ChatRoom.svelte          # 1:1 채팅방
│   ├── MessageInput.svelte      # 메시지 입력 영역
│   └── TypingIndicator.svelte   # 타이핑 표시
│
├── Broadcast/
│   ├── BroadcastCard.svelte     # 브로드캐스트 카드
│   ├── BroadcastFeed.svelte     # 브로드캐스트 피드
│   ├── BroadcastCreate.svelte   # 브로드캐스트 생성
│   └── RecipientBadge.svelte    # 수신자 상태
│
├── User/
│   ├── ProfileCard.svelte       # 프로필 카드 (익명)
│   ├── MannerTemp.svelte        # 매너 온도 표시
│   ├── OnlineStatus.svelte      # 온라인/오프라인 표시
│   └── BlockButton.svelte       # 차단 버튼
│
└── Admin/
    ├── StatsCard.svelte         # 통계 카드
    ├── DataTable.svelte         # 데이터 테이블
    ├── ReportItem.svelte        # 신고 항목
    └── UserManagement.svelte    # 사용자 관리
```

---

## 3. 기술 스택 상세

### 3.1 추가 의존성

**Gemfile (Ruby)**:
```ruby
# Inertia.js Rails adapter
gem "inertia_rails", "~> 3.0"

# Vite Rails integration
gem "vite_rails", "~> 3.0"
```

**package.json (Node)**:
```json
{
  "devDependencies": {
    "vite": "^6.0",
    "vite-plugin-ruby": "^5.0",
    "@sveltejs/vite-plugin-svelte": "^5.0",
    "svelte": "^5.0",
    "@inertiajs/svelte": "^2.0",
    "tailwindcss": "^4.0",
    "@tailwindcss/vite": "^4.0",
    "autoprefixer": "^10.0",
    "postcss": "^8.0"
  },
  "dependencies": {
    "axios": "^1.7",
    "@inertiajs/progress": "^0.2"
  }
}
```

### 3.2 디렉토리 구조 (신규)

```
talkk/api/
├── app/
│   ├── controllers/
│   │   ├── api/                    # [유지] 기존 API 컨트롤러
│   │   │   └── v1/
│   │   │       ├── broadcasts_controller.rb
│   │   │       ├── conversations_controller.rb
│   │   │       └── users_controller.rb
│   │   ├── web/                    # [신규] Inertia 웹 컨트롤러
│   │   │   ├── base_controller.rb
│   │   │   ├── home_controller.rb
│   │   │   ├── auth_controller.rb
│   │   │   ├── broadcasts_controller.rb
│   │   │   ├── conversations_controller.rb
│   │   │   ├── profile_controller.rb
│   │   │   ├── notifications_controller.rb
│   │   │   └── settings_controller.rb
│   │   └── admin/                  # [리팩토링] 관리자 → Inertia
│   │       ├── dashboard_controller.rb
│   │       ├── users_controller.rb
│   │       └── reports_controller.rb
│   │
│   ├── frontend/                   # [신규] Svelte 5 프론트엔드
│   │   ├── entrypoints/
│   │   │   └── application.js      # Inertia + Svelte 초기화
│   │   ├── pages/                  # Inertia 페이지 컴포넌트
│   │   │   ├── Home/
│   │   │   │   └── Index.svelte    # 메인 홈 (브로드캐스트 피드)
│   │   │   ├── Auth/
│   │   │   │   ├── Login.svelte    # 로그인 (폰번호)
│   │   │   │   ├── Verify.svelte   # SMS 인증
│   │   │   │   └── Register.svelte # 프로필 설정
│   │   │   ├── Broadcasts/
│   │   │   │   ├── Index.svelte    # 받은 브로드캐스트 목록
│   │   │   │   ├── Show.svelte     # 브로드캐스트 상세
│   │   │   │   └── Create.svelte   # 새 브로드캐스트
│   │   │   ├── Conversations/
│   │   │   │   ├── Index.svelte    # 대화 목록
│   │   │   │   └── Show.svelte     # 1:1 채팅방
│   │   │   ├── Profile/
│   │   │   │   ├── Show.svelte     # 내 프로필
│   │   │   │   └── Edit.svelte     # 프로필 수정
│   │   │   ├── Notifications/
│   │   │   │   └── Index.svelte    # 알림 센터
│   │   │   ├── Settings/
│   │   │   │   └── Index.svelte    # 설정
│   │   │   └── Admin/
│   │   │       ├── Dashboard.svelte # 관리자 대시보드
│   │   │       ├── Users.svelte     # 사용자 관리
│   │   │       └── Reports.svelte   # 신고 관리
│   │   ├── components/             # 재사용 컴포넌트
│   │   │   ├── layout/
│   │   │   ├── common/
│   │   │   ├── voice/
│   │   │   ├── chat/
│   │   │   ├── broadcast/
│   │   │   ├── user/
│   │   │   └── admin/
│   │   ├── lib/                    # 유틸리티
│   │   │   ├── stores.js           # Svelte stores (auth, theme)
│   │   │   ├── api.js              # API 클라이언트
│   │   │   ├── utils.js            # 헬퍼 함수
│   │   │   └── constants.js        # 상수 정의
│   │   └── styles/
│   │       └── app.css             # Tailwind 진입점
│   │
│   ├── models/                     # [유지] 기존 모델
│   ├── services/                   # [유지] 기존 서비스
│   └── views/
│       └── layouts/
│           └── application.html.erb # Vite + Inertia 루트 레이아웃
│
├── config/
│   ├── routes.rb                   # [수정] 웹 라우트 추가
│   ├── initializers/
│   │   └── inertia_rails.rb        # [신규] Inertia 설정
│   └── vite.json                   # [신규] Vite 설정
│
├── vite.config.ts                  # [신규] Vite + Svelte 설정
├── tailwind.config.js              # [신규] Tailwind 설정
├── postcss.config.js               # [신규] PostCSS 설정
└── package.json                    # [신규] Node 의존성
```

---

## 4. 라우팅 설계

### 4.1 Web Routes (Inertia)

```ruby
# config/routes.rb 추가 부분

# ============================================================
# Web Routes (Inertia.js - Svelte 5 Frontend)
# ============================================================

# 인증 플로우
scope :auth do
  get "login",     to: "web/auth#login",    as: :login
  post "login",    to: "web/auth#create_session"
  get "verify",    to: "web/auth#verify"
  post "verify",   to: "web/auth#verify_code"
  get "register",  to: "web/auth#register"
  post "register", to: "web/auth#create_user"
  delete "logout", to: "web/auth#destroy"
end

# 메인 페이지
root "web/home#index"

# 브로드캐스트
scope :web do
  resources :broadcasts, controller: "web/broadcasts", only: [:index, :show, :new, :create]

  # 대화
  resources :conversations, controller: "web/conversations", only: [:index, :show] do
    member do
      post :send_message
      post :favorite
    end
  end

  # 프로필
  resource :profile, controller: "web/profile", only: [:show, :edit, :update]

  # 알림
  resources :notifications, controller: "web/notifications", only: [:index] do
    collection do
      patch :mark_all_read
    end
  end

  # 설정
  resource :settings, controller: "web/settings", only: [:show, :update]
end

# 관리자 (Inertia)
namespace :admin do
  root to: "dashboard#index"
  resources :users, only: [:index, :show] do
    member do
      post :suspend
      post :unsuspend
    end
  end
  resources :reports, only: [:index, :show] do
    member do
      post :process_report
      post :reject_report
    end
  end
  get "analytics", to: "analytics#index"
end
```

### 4.2 기존 API Routes (유지)

```ruby
# 기존 /api/v1/* 라우트는 100% 유지
# Flutter 앱과의 하위 호환성 보장
namespace :api do
  namespace :v1 do
    # ... (기존 라우트 그대로)
  end
end
```

---

## 5. 페이지별 UI 설계

### 5.1 로그인 페이지 (`Auth/Login.svelte`)

```
┌─────────────────────────────────────┐
│                                     │
│     ☁ [앱 로고 - 하늘색 그라디언트]    │
│                                     │
│        T A L K K                    │
│     "음성으로 연결되는 세상"            │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🇰🇷 +82  010-0000-0000     │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │         인증번호 요청          │   │  ← sky-500 그라디언트 버튼
│  └─────────────────────────────┘   │
│                                     │
│     이용약관  |  개인정보처리방침       │
│                                     │
└─────────────────────────────────────┘
```

### 5.2 메인 홈 (`Home/Index.svelte`)

```
┌──────────────────────────────────────────────────────┐
│ [Header]                                              │
│  ☁ Talkk        🔔 3    ⚙                           │
├──────────┬───────────────────────────────────────────┤
│ [Sidebar] │  [Main Content]                           │
│           │                                           │
│  🏠 홈    │  ┌─ 새 브로드캐스트 ─────────────────┐   │
│  📢 방송  │  │  🎙️ 60초 음성으로 이야기하세요      │   │
│  💬 채팅  │  │  [          녹음 시작          ]   │   │
│  👤 프로필 │  └──────────────────────────────────┘   │
│  🔔 알림  │                                           │
│  ⚙ 설정   │  ┌─ 받은 브로드캐스트 ──────────────┐   │
│           │  │                                    │   │
│           │  │  🦊 익명의 여우   ·  2분 전         │   │
│           │  │  ▶ ━━━━━━━━━━━━━  0:42           │   │
│           │  │  [답장하기]  ❤️ 12                  │   │
│           │  │                                    │   │
│           │  ├────────────────────────────────────┤   │
│           │  │                                    │   │
│           │  │  🐻 익명의 곰    ·  15분 전        │   │
│           │  │  ▶ ━━━━━━━━━━━━━  1:00           │   │
│           │  │  [답장하기]  ❤️ 8                   │   │
│           │  │                                    │   │
│           │  └──────────────────────────────────┘   │
│           │                                           │
├──────────┴───────────────────────────────────────────┤
│ [BottomNav - 모바일에서만 표시]                         │
│  🏠     📢     🎙     💬     👤                       │
└──────────────────────────────────────────────────────┘
```

### 5.3 대화 목록 (`Conversations/Index.svelte`)

```
┌──────────────────────────────────┐
│  💬 대화                   검색 🔍 │
├──────────────────────────────────┤
│  ⭐ 즐겨찾기                       │
│  ┌──────────────────────────┐   │
│  │ 🦊 익명의 여우    3분 전  ● │   │  ← cyan-400 온라인 dot
│  │ 🔊 음성 메시지 1건         │   │
│  │ 🌡 매너온도 36.5°         │   │
│  └──────────────────────────┘   │
│                                  │
│  💬 전체 대화                     │
│  ┌──────────────────────────┐   │
│  │ 🐻 익명의 곰     1시간 전   │   │
│  │ "재밌는 이야기네요!"        │   │
│  │ 읽지 않은 메시지 2         │   │  ← sky-500 뱃지
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │ 🐧 익명의 펭귄   어제      │   │
│  │ 🔊 음성 메시지             │   │
│  └──────────────────────────┘   │
└──────────────────────────────────┘
```

### 5.4 채팅방 (`Conversations/Show.svelte`)

```
┌──────────────────────────────────────┐
│ ← 🦊 익명의 여우   🌡36.5°  ⋮       │
├──────────────────────────────────────┤
│                                      │
│        📢 브로드캐스트에서 시작됨       │
│        ───────────────────           │
│                                      │
│    ┌──────────────────┐              │
│    │ ▶ ━━━━━━━  0:42  │   2:30 PM   │  ← 상대방 음성
│    └──────────────────┘              │
│                                      │
│              ┌──────────────────┐    │
│   3:15 PM    │ ▶ ━━━━━━━  0:30  │   │  ← 내 음성 (sky-500 배경)
│              └──────────────────┘    │
│                                      │
│    안녕하세요! 반갑습니다 😊            │  ← 상대방 텍스트
│                                      │
├──────────────────────────────────────┤
│  ┌────────────────────────┐  🎙  📎  │
│  │ 메시지를 입력하세요...    │         │
│  └────────────────────────┘   ➤     │
└──────────────────────────────────────┘
```

### 5.5 관리자 대시보드 (`Admin/Dashboard.svelte`)

```
┌──────────────────────────────────────────────────────┐
│  ⚙ Talkk Admin                            관리자님 👤 │
├──────────┬───────────────────────────────────────────┤
│          │                                            │
│ 📊 대시보드│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│
│ 👥 사용자 │  │ 전체 유저 │ │ 오늘 가입│ │ 방송 수 │ │ 신고 수 ││
│ 🚨 신고   │  │  1,234  │ │   23   │ │  456  │ │  12   ││
│ 📈 분석   │  └────────┘ └────────┘ └────────┘ └────────┘│
│          │                                            │
│          │  ┌─ 최근 활동 ────────────────────────┐    │
│          │  │  📊 일별 활성 사용자 차트              │    │
│          │  │  [=============================]    │    │
│          │  │  [============================]     │    │
│          │  └────────────────────────────────┘    │
│          │                                            │
│          │  ┌─ 미처리 신고 ─────────────────────┐    │
│          │  │ 🚨 "부적절한 음성 메시지" - 2시간 전   │    │
│          │  │    [처리]  [기각]                    │    │
│          │  │ ⚠️ "스팸 의심" - 5시간 전            │    │
│          │  │    [처리]  [기각]                    │    │
│          │  └────────────────────────────────┘    │
└──────────┴───────────────────────────────────────────┘
```

---

## 6. 구현 단계

### Phase 1: 기반 설정 (Day 1)

**1.1 Rails Full-Stack 전환**
- [ ] `config/application.rb`: API-only 모드 해제
- [ ] View 관련 middleware 활성화
- [ ] `app/views/layouts/application.html.erb` 생성

**1.2 Vite + Svelte 5 설정**
- [ ] `package.json` 생성 및 의존성 설치
- [ ] `vite.config.ts` 설정 (Svelte 5 + Tailwind 4)
- [ ] `config/vite.json` 설정
- [ ] `tailwind.config.js` + `postcss.config.js`

**1.3 Inertia.js 통합**
- [ ] `inertia_rails` gem 설치 및 설정
- [ ] `config/initializers/inertia_rails.rb` 생성
- [ ] `app/frontend/entrypoints/application.js` 진입점

**1.4 기본 레이아웃**
- [ ] `AppShell.svelte` (반응형 셸)
- [ ] `Sidebar.svelte` / `BottomNav.svelte`
- [ ] `Header.svelte`
- [ ] Tailwind 커스텀 테마 (sky blue)

### Phase 2: 인증 플로우 (Day 2)

**2.1 Web Auth Controllers**
- [ ] `Web::AuthController` (Inertia 렌더링)
- [ ] Session 기반 인증 (웹) vs JWT (API) 분리
- [ ] CSRF 보호 활성화 (웹 라우트)

**2.2 Auth Pages**
- [ ] `Auth/Login.svelte` - 전화번호 입력
- [ ] `Auth/Verify.svelte` - SMS 인증
- [ ] `Auth/Register.svelte` - 프로필 설정 (닉네임, 성별, 연령대)
- [ ] 인증 미들웨어 (Inertia redirect)

### Phase 3: 메인 페이지 (Day 3-4)

**3.1 홈 & 브로드캐스트**
- [ ] `Home/Index.svelte` - 메인 피드
- [ ] `Broadcasts/Index.svelte` - 받은 브로드캐스트
- [ ] `Broadcasts/Show.svelte` - 브로드캐스트 상세
- [ ] `Broadcasts/Create.svelte` - 새 브로드캐스트

**3.2 음성 컴포넌트**
- [ ] Web Audio API 기반 VoiceRecorder
- [ ] WaveSurfer.js 기반 VoicePlayer
- [ ] 실시간 파형 시각화

**3.3 대화**
- [ ] `Conversations/Index.svelte` - 대화 목록
- [ ] `Conversations/Show.svelte` - 채팅방
- [ ] 실시간 메시지 (ActionCable or polling)

### Phase 4: 부가 기능 (Day 5)

**4.1 프로필 & 설정**
- [ ] `Profile/Show.svelte` - 내 프로필
- [ ] `Profile/Edit.svelte` - 프로필 수정
- [ ] `Settings/Index.svelte` - 알림 설정, 차단 관리

**4.2 알림**
- [ ] `Notifications/Index.svelte` - 알림 센터
- [ ] 실시간 알림 카운터 (Header 뱃지)

### Phase 5: 관리자 대시보드 (Day 6)

- [ ] `Admin/Dashboard.svelte` - 통계 개요
- [ ] `Admin/Users.svelte` - 사용자 관리
- [ ] `Admin/Reports.svelte` - 신고 관리
- [ ] 차트 라이브러리 (Chart.js or Apache ECharts)

### Phase 6: 다크모드 & 폴리싱 (Day 7)

- [ ] 다크모드 토글 (Tailwind dark class strategy)
- [ ] 애니메이션 & 트랜지션 (Svelte transitions)
- [ ] 반응형 최적화 (모바일 ↔ 데스크톱)
- [ ] 접근성 (ARIA, 키보드 네비게이션)
- [ ] 성능 최적화 (이미지 lazy loading, 코드 스플리팅)

---

## 7. 핵심 설정 파일

### 7.1 `vite.config.ts`

```typescript
import { defineConfig } from "vite";
import { svelte } from "@sveltejs/vite-plugin-svelte";
import RubyPlugin from "vite-plugin-ruby";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [
    RubyPlugin(),
    svelte(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      "$components": "/app/frontend/components",
      "$lib": "/app/frontend/lib",
      "$pages": "/app/frontend/pages",
    },
  },
});
```

### 7.2 `app/frontend/entrypoints/application.js`

```javascript
import { createInertiaApp } from "@inertiajs/svelte";
import "../styles/app.css";

createInertiaApp({
  resolve: (name) => {
    const pages = import.meta.glob("../pages/**/*.svelte", { eager: true });
    return pages[`../pages/${name}.svelte`];
  },
  setup({ el, App }) {
    new App({ target: el });
  },
});
```

### 7.3 `config/initializers/inertia_rails.rb`

```ruby
InertiaRails.configure do |config|
  config.version = ViteRuby.digest
  config.ssr_enabled = false

  config.default_render = true
end
```

---

## 8. 데이터 흐름

### 8.1 Inertia 페이지 렌더링

```
[브라우저 요청]
    ↓
[Rails Router] → web/broadcasts#index
    ↓
[Web::BroadcastsController]
    ↓ render inertia: "Broadcasts/Index", props: { broadcasts: @broadcasts }
    ↓
[Inertia Middleware] → JSON response with page component + props
    ↓
[Svelte App] → renders Broadcasts/Index.svelte with props
    ↓
[사용자 화면]
```

### 8.2 API vs Web 컨트롤러 분리

```ruby
# API Controller (기존 유지 - JSON 응답)
class Api::V1::BroadcastsController < Api::BaseController
  def index
    broadcasts = current_user.received_broadcasts.active
    render json: broadcasts
  end
end

# Web Controller (신규 - Inertia 렌더링)
class Web::BroadcastsController < Web::BaseController
  def index
    broadcasts = current_user.received_broadcasts
      .active
      .includes(:user)
      .order(created_at: :desc)
      .page(params[:page])

    render inertia: "Broadcasts/Index", props: {
      broadcasts: broadcasts.map { |b| serialize_broadcast(b) },
      pagination: pagination_meta(broadcasts)
    }
  end
end
```

---

## 9. 리스크 & 완화 전략

| 리스크 | 영향 | 완화 전략 |
|--------|------|----------|
| API-only → Full-stack 전환 시 기존 API 깨짐 | 높음 | API namespace 완전 분리, 테스트 유지 |
| Web Audio API 브라우저 호환성 | 중간 | 폴리필 + 호환성 매트릭스 확인 |
| 인증 이중화 (JWT + Session) 복잡도 | 중간 | 명확한 미들웨어 분리, 공통 인증 서비스 |
| Svelte 5 + Inertia.js 안정성 | 낮음 | @inertiajs/svelte v2 안정 버전 사용 |
| 성능 (SSR 없이 Inertia SPA) | 낮음 | CSR 최적화, 코드 스플리팅 |

---

## 10. 성공 기준

- [ ] 기존 Flutter API 100% 하위 호환
- [ ] 웹 브라우저에서 음성 녹음/재생 동작
- [ ] 모바일 퍼스트 반응형 (320px ~ 1440px)
- [ ] Lighthouse Performance Score > 90
- [ ] 다크모드 완전 지원
- [ ] 관리자 대시보드 기능 완료
- [ ] 페이지 전환 < 300ms (Inertia SPA)

---

## Sources & References
- [Whispr 공식 사이트](https://www.whispr.co.kr/)
- [Whispr - App Store](https://apps.apple.com/kr/app/%EC%9C%84%EC%8A%A4%ED%8D%BC-%EB%AA%A9%EC%86%8C%EB%A6%AC%ED%86%A1-%EC%9D%B5%EB%AA%85-%EC%9D%8C%EC%84%B1-%EB%8C%80%ED%99%94/id6742844118)
- [Whispr - Google Play](https://play.google.com/store/apps/details?id=kr.co.whispr.app)
- [Inertia.js Documentation](https://inertiajs.com/)
- [Svelte 5 Documentation](https://svelte.dev/)
- [Tailwind CSS v4](https://tailwindcss.com/)
